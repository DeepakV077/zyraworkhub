// Intentionally minimal ESM flat config file left empty because the project
// uses `eslint.config.cjs` (CommonJS) as the authoritative flat configuration.
// Keeping this file avoids accidental ESM/require resolution issues but ensures
// ESLint won't see any legacy 'extends' entries here.

export default [];
